[package]
name = "calibre-gen2-perf019"
version = "1.9.0"
edition = "2024"

[dependencies]
ed25519-dalek = { version = "2.2.0", features = ["batch"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
